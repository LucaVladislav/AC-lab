# Monitoring and logging

The Kubernetes application can be monitored using kubectl commands.

Used commands:

```bash
kubectl get pods
kubectl get deployments
kubectl get services
kubectl logs deployment/ac-lab-app
kubectl describe deployment ac-lab-app
kubectl top nodes
kubectl top pods

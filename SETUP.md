# Environment setup

For this project, the Ubuntu server environment was prepared with the following tools:

- Git
- Java 17
- Docker
- Docker Compose

The tools were verified using:

```bash
java -version
git --version
docker --version
docker compose version
docker run hello-world
